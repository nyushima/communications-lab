function setup() {
  createCanvas(800, 500);
  background(255,0,0);
  // background(w);       // you can change the background color in grayscale color space.
  // background(r,g,b);   // you can change the background color in RGB color space.
}

function draw() {

}
